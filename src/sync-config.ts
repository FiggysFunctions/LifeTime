// The Dexie Cloud database this app syncs against. Public identifier —
// safe to commit (access is controlled by login, not by knowing the URL).
// Empty string = sync not set up; the app stays fully local.
export const DEXIE_CLOUD_URL = "https://z0oc9l08c.dexie.cloud";
